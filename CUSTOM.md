# 自定义修改记录

本文件记录所有自定义修改的动机和决策，供后续合并上游代码时参考。

## 修改总览

### 1. 彻底关闭 Langfuse 追踪

**动机**：隐私考量。Langfuse 会将完整的对话内容、LLM 响应文本发送到 langfuse.com，不希望对话数据泄露给除 LLM Provider 以外的第三方。

**决策**：不依赖环境变量开关，直接在代码层面彻底关闭。即便上游或运维人员设置了相关环境变量，Langfuse 客户端也不会初始化，所有追踪方法均为空操作。

**合并指导**：上游对 Langfuse 追踪系统的任何更新（新增功能、修改参数、升级版本）都不应被采纳，应保持本地彻底关闭状态。如果上游新增其他遥测 / 追踪渠道，需以同样态度评估和处理。

### 2. 关闭 trace 事件 API

**动机**：与 Langfuse 配套的前端事件上报（复制消息、修改消息、重新生成消息评分），虽依赖 Langfuse 客户端，但为避免将来意外激活，直接关闭端点。

**决策**：trace 路由端点直接返回成功响应，不处理请求体。

**合并指导**：上游对此端点的任何增强或重构都不需要采纳。

### 3. 自定义 ARM64 Docker 构建流程

**动机**：个人部署服务器为 ARM64 架构，需要构建适配的 Docker 镜像，且不需要与上游同步。

**决策**：创建独立的 GitHub Actions 工作流，推送 custom 分支时触发构建，镜像推送到个人的 GitHub Container Registry。不启用上游自动同步。

**合并指导**：上游对官方构建流程的修改（release-docker.yml 等）不影响自定义工作流。如需要，可手动参考上游更新。

### 4. 移除首页 SuggestQuestions 建议问题

**动机**：这是随机展示的推荐问题列表，个人自用不需要，希望首页更简洁。

**决策**：从 InputArea 组件中直接删除该模块的引用和渲染逻辑。

### 5. 首页内容垂直居中

**动机**：类似 ChatGPT 登录页，希望首页内容在垂直方向居中，而不是偏向上方。

**决策**：外层容器添加垂直 flex 居中，移除原 `paddingBlock` 偏移量。

### 6. 移除首页 StarterList 快速入口按钮

**动机**：三个按钮（DeepSeek V4 Pro、图片生成、视频生成）依赖 LobeHub 云服务提供的特定模型，个人自用没有这些模型，按钮无实际功能。

**决策**：从 InputArea 组件中删除 StarterList 引用和渲染。图片 / 视频生成仍可通过直接 URL 访问。

**合并指导**：上游对 StarterList 的任何更新不需要采纳。

### 7. 修复 Docker 构建中 pnpm init 兼容性问题

**动机**：`pnpm init` 自动生成 `devEngines` 字段导致后续 `pnpm add` 因 semver 校验失败。

**决策**：用 `printf '{"private":true}' > package.json` 手工创建最小 manifest，绕过 pnpm init 的副作用。

**合并指导**：上游 Dockerfile 此处的任何修改优先保留本地方案。

### 8. AI 服务商设置页交互优化

**动机**：个人使用时频繁调整 AI 服务商配置。上游实现使用 provider 级配置保存 loading，修改 API Key、客户端请求模式、Responses API 或连通性检查模型时，会让同一服务商下多个无关控件同时出现 loading，视觉干扰较大。

**决策**：保留 provider 级 loading 兼容现有流程，同时新增字段级 loading。API Key、Base URL、客户端请求模式、Responses API、连通性检查模型分别显示自己的保存状态，避免改一个字段时整组控件一起 loading。

**合并指导**：上游若重构 AI 服务商配置保存逻辑，应继续保留 “字段级 loading” 的用户体验；不要退回到同 provider 下所有配置项共用一个 loading。

### 9. 模型列表批量开关常驻显示

**动机**：希望在服务商模型列表中可以快速批量启用或禁用当前分类下的模型，不需要先通过搜索或其它菜单进入批量操作。

**决策**：将 “已启用” 区域原本的批量禁用图标改为常驻小开关。开关打开时启用当前 tab 范围内全部可切换模型，关闭时禁用当前 tab 范围内全部可切换模型；开关状态根据当前范围内是否存在已启用模型显示。

**合并指导**：上游若调整模型列表头部操作，应保留该常驻批量开关能力，并继续让作用范围跟随当前模型 tab。
